spring:
  ai:
    model:
      chat: anthropic
      embedding: openai
    anthropic:
      api-key: test-key
    openai:
      api-key: test-key

rag:
  store-path: ./build/test-data/vector-store.json
